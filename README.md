Build a clean production-ready YouTube Approval Layer app by fixing and wiring my existing MERN backend and React 
frontend together (auth, invite editors, video upload, approval → auto upload to YouTube using refresh token, protected routes, JWT middlewares, proper .env usage, Zod validation, error handling, clean folder structure, consistent API paths, and persistent login). Keep code SIMPLE + readable (comment every important line), do not remove features we already built, but safely refactor broken parts, remove unused junk files, and make sure signup/signin + pending videos + approve/reject + invite editor all work end-to-end. If something is ambiguous, infer best practices instead of asking — but never break existing logic. Add minimal Tailwind UI, keep routes exactly /api/v1/user, /api/v1/admin, /api/v1/videos, /api/editor. Finally test everything locally and ensure no 404, CORS, or bcrypt/JWT bugs remain, and write short README explaining how to run both frontend and backend.   